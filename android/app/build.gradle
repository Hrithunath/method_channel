plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.method_channel"
    compileSdk = 34 // Updated to 34 as per the error suggestion
    ndkVersion = "25.2.9519653"

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8" // Corrected syntax
    }

    defaultConfig {
        applicationId = "com.example.method_channel"
        minSdk = 21
        targetSdk = 34 // Optionally updated to 34 to match compileSdk
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.." // Ensure this path correctly points to your Flutter project directory
}
